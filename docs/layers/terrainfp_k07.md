# Terrain Fingerprint (mag+baro+cell+wifi)

`terrainfp_k07` · Layer 84 · Group K — Systems Intelligence

> Multi-sensor fingerprint map matching for GPS-denied nav

## Current status

**PLACEHOLDER** — Returns the same fixed reading whenever nothing is connected. Not random, but not measured either.

Measured by constructing this layer with nothing attached and reading it three times: returns 13.08270, 80.27070 every time, with no input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `TerrainFingerprintLayer` |
| Source | [`upin/layers/systems/terrain_fp_layer.py`](../../upin/layers/systems/terrain_fp_layer.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.55 |
| Novel | yes |

## How it works

```
Layer 84 — Multi-sensor Terrain Fingerprint Matching.

Unique mag+baro+cell+wifi signature per location; matches current
sensor readings against the stored fingerprint map.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
