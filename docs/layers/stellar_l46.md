# Stellar Constellation Pattern Navigation

`stellar_l46` · Layer 46 · Group A — Satellite & Celestial

> Star pattern recognition for heading determination

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned heading 42.6 deg, then a different heading on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `StellarConstellationLayer` |
| Source | [`upin/layers/satellite/layers.py`](../../upin/layers/satellite/layers.py) |
| Capabilities | HEADING |
| Accuracy rating | 0.65 |
| Novel | yes |
| Bio-inspiration | Bogong moth stellar navigation |

## How it works

```
Layer 46 — Stellar Constellation Pattern Navigation [NOVEL].

Star constellation pattern recognition for heading and direction.
Inspired by the Bogong moth (confirmed Nature, June 2025).
More robust than single-star tracking.

Physics chain:
    world.get_star_positions() → star pattern matching → heading
This is a heading-only layer; it does not produce a position fix.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
