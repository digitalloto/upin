# Quantum Gravity Gradiometer

`gravgrad_l28a` · Layer 28 · Group G — Gravity

> Atom interferometry gravity gradient — detects underground structures

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08182, 80.27142, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `QuantumGravityGradiometerLayer` |
| Source | [`upin/layers/gravity/layers.py`](../../upin/layers/gravity/layers.py) |
| Capabilities | POSITION, ENVIRONMENT |
| Accuracy rating | 0.60 |
| Novel | yes |

## How it works

```
Layer 28a — Quantum Gravity Gradiometer [NOVEL, UNDERWATER].

Atom interferometry measures gravitational gradient variations.
Detects underground structures — Univ. Birmingham: 2m tunnel at 19cm accuracy.
Also provides position via gravitational map matching.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
