# Salmon Magnetic+Chemical Homing

`salmon_h08` · Layer 71 · Group H — Chemical, Seismic & Flow

> Combined magnetic map matching and chemical gradient following

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08269, 80.27060, heading 16.4 deg, then a different position and heading on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `SalmonHomingLayer` |
| Source | [`upin/layers/biological/layers.py`](../../upin/layers/biological/layers.py) |
| Capabilities | POSITION, HEADING |
| Accuracy rating | 0.50 |
| Novel | yes |
| Bio-inspiration | Salmon bicoordinate magnetic + olfactory homing |

## How it works

```
Salmon magnetic + chemical homing — combined navigation.

Salmon navigate thousands of kilometres using:
- Magnetic map matching (intensity + inclination = bicoordinate)
- Olfactory gradient following (chemical trail homing)
- 60% magnetic weight + 40% chemical weight

This combines both modalities into a single fused layer,
unlike the separate magano_l6 and chemgrad_l26 layers.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
