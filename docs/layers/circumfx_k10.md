# Circumference Intersection

`circumfx_k10` · Layer 87 · Group K — Systems Intelligence

> Multi-circle circumference crossing point = position

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08309, 80.27066, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `CircumferenceIntersectionLayer` |
| Source | [`upin/layers/systems/circumference_layer.py`](../../upin/layers/systems/circumference_layer.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.70 |
| Novel | yes |

## How it works

```
Layer 87 — Circumference Intersection Position.

Finds the point where multiple tower circle circumferences cross.
With GPS-calibrated exact radii, all circles pass through your
position. The intersection point IS your location.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
