# Thermal Microclimate Fingerprint

`thermalmicro_h13` · Layer 133 · Group H — Chemical, Seismic & Flow

> Position from thermal diurnal cycle fingerprint

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08016, 80.26875, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `ThermalMicroclimateLayer` |
| Source | [`upin/layers/exotic/frontier_nav.py`](../../upin/layers/exotic/frontier_nav.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.25 |
| Novel | yes |

## How it works

```
Thermal microclimate signature — every location has a diurnal heat fingerprint.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
