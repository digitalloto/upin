# Diffuse Sky Brightness Gradient Navigation

`skygrad_l47` · Layer 47 · Group A — Satellite & Celestial

> Sky brightness gradient for heading

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned heading 43.2 deg, then a different heading on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `DiffuseSkyLayer` |
| Source | [`upin/layers/satellite/layers.py`](../../upin/layers/satellite/layers.py) |
| Capabilities | HEADING |
| Accuracy rating | 0.50 |
| Novel | yes |
| Bio-inspiration | Dung beetle Milky Way navigation |

## How it works

```
Layer 47 — Diffuse Sky Brightness Gradient Navigation [NOVEL].

Night sky brightness gradient including Milky Way diffuse light
for heading derivation. Inspired by dung beetle Milky Way navigation.
Works when individual stars are not identifiable.

Physics chain:
    world.true_heading + sensor noise → heading
Lowest accuracy heading source but works in poor visibility.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
