# RF Environment Fingerprint

`rffingerprint_k12` · Layer 120 · Group K — Systems Intelligence

> Full RF spectrum signature matching — location from RF fingerprint

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08287, 80.27072, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `RFEnvironmentFingerprintLayer` |
| Source | [`upin/layers/rf/cellular_advanced.py`](../../upin/layers/rf/cellular_advanced.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.55 |
| Novel | yes |

## How it works

```
RF environment fingerprinting — full spectrum signature = location.

Characterise a location by its complete RF spectrum: WiFi + cell +
LoRa + broadcast combined as features. Each location has a unique RF fingerprint.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
