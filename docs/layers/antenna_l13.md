# Antenna Stabilisation

`antenna_l13` · Layer 13 · Group K — Systems Intelligence

> Antenna pointing auto-correction on GPS compromise

## Current status

**PLACEHOLDER** — Returns the same fixed reading whenever nothing is connected. Not random, but not measured either.

Measured by constructing this layer with nothing attached and reading it three times: returns heading 0.0 deg every time, with no input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `AntennaStabilisationLayer` |
| Source | [`upin/layers/systems/layers.py`](../../upin/layers/systems/layers.py) |
| Capabilities | HEADING |
| Accuracy rating | 0.70 |
| Novel | yes |

## How it works

```
Layer 13 — Antenna Stabilisation Layer [NOVEL].

UPIN position output feeds antenna pointing system. When GPS is
compromised, antenna auto-corrects using alternative sources.
Prevents cascade failure: GPS spoofing -> antenna misalignment -> datalink loss.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
