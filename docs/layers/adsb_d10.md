# ADS-B Cooperative Aviation

`adsb_d10` · Layer 101 · Group D — RF & Terrestrial

> ADS-B aircraft broadcast — cooperative aviation positioning

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08271, 80.27064, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `ADSBCooperativeLayer` |
| Source | [`upin/layers/satellite/aviation_nav.py`](../../upin/layers/satellite/aviation_nav.py) |
| Capabilities | POSITION, ENVIRONMENT |
| Accuracy rating | 0.70 |
| Novel | no |

## How it works

```
ADS-B — aircraft broadcast position/velocity continuously.

Receiving ADS-B from nearby aircraft gives cooperative reference
points. Like AIS but for aviation.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
