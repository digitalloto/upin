# Predictive Tower Verification

`predtower_k08` · Layer 85 · Group K — Systems Intelligence

> Predict tower distances T+3s to T+5m, verify with RSSI

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08246, 80.27081, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `PredictiveTowerVerificationLayer` |
| Source | [`upin/layers/systems/predictive_tower_layer.py`](../../upin/layers/systems/predictive_tower_layer.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.60 |
| Novel | yes |

## How it works

```
Layer 85 — Predictive Tower Verification.

Predicts tower distances at T+3s to T+5m, then verifies against
actual RSSI changes. Validated predictions prove the motion model
works → trusted during GPS denial.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
