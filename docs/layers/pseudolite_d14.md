# Pseudolite Ground GNSS

`pseudolite_d14` · Layer 106 · Group D — RF & Terrestrial

> Ground-based GPS-like transmitters — synthetic GNSS

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08270, 80.27071, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `PseudoliteLayer` |
| Source | [`upin/layers/satellite/aviation_nav.py`](../../upin/layers/satellite/aviation_nav.py) |
| Capabilities | POSITION, TIMING |
| Accuracy rating | 0.80 |
| Novel | yes |

## How it works

```
Pseudolite — ground-based GPS-like transmitters.

Synthetic GNSS constellation on the ground. Defence and remote-area
use where satellite coverage is denied or insufficient.
Conceptually aligned with GARUDA-as-anchor thinking.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
