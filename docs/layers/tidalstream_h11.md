# Tidal Stream Atlas

`tidalstream_h11` · Layer 99 · Group H — Chemical, Seismic & Flow

> Published predicted tidal stream vectors for DR correction

## Current status

**PLACEHOLDER** — Returns the same fixed reading whenever nothing is connected. Not random, but not measured either.

Measured by constructing this layer with nothing attached and reading it three times: returns 0.80 m/s every time, with no input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `TidalStreamAtlasLayer` |
| Source | [`upin/layers/maritime/classical_nav.py`](../../upin/layers/maritime/classical_nav.py) |
| Capabilities | VELOCITY |
| Accuracy rating | 0.50 |
| Novel | no |

## How it works

```
Tidal stream atlas — predicted tidal currents from published tables.

Distinct from ocean current (h_09): tidal streams are PREDICTED/PUBLISHED
with hour-by-hour vectors. Used for dead reckoning correction.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
