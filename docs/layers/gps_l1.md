# GPS GNSS

`gps_l1` · Layer 1 · Group A — Satellite & Celestial

> Standard GPS L1/L2 satellite positioning

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08267, 80.27074, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `GPSLayer` |
| Source | [`upin/layers/satellite/layers.py`](../../upin/layers/satellite/layers.py) |
| Capabilities | POSITION, VELOCITY, TIMING |
| Accuracy rating | 0.70 |
| Novel | no |

## How it works

```
Layer 1 — GPS GNSS.

Standard global navigation satellite system. Provides positioning
through timing of radio signals from MEO satellites (~20,000 km).
Vulnerable to jamming and spoofing — signals arrive below thermal noise.

Physics chain:
    world.get_pseudoranges("GPS") → trilateration least-squares → (lat, lon, alt)
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
