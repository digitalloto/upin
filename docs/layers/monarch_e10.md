# Monarch Sun Compass

`monarch_e10` · Layer 66 · Group E — Optical & Vision

> Solar azimuth + circadian clock heading estimation

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned heading 44.4 deg, then a different heading on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `MonarchSunCompassLayer` |
| Source | [`upin/layers/optical/layers.py`](../../upin/layers/optical/layers.py) |
| Capabilities | HEADING |
| Accuracy rating | 0.50 |
| Novel | yes |
| Bio-inspiration | Monarch butterfly time-compensated sun compass |

## How it works

```
Sun compass heading — inspired by Monarch butterfly migration.

Monarchs use a time-compensated sun compass: they combine the sun's
azimuth with an internal circadian clock to maintain a constant
heading across 4000 km of migration.  This layer measures solar
azimuth + time-of-day to derive true heading.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
