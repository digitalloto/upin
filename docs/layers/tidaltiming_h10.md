# Tidal Timing Position

`tidaltiming_h10` · Layer 83 · Group H — Chemical, Seismic & Flow

> Coastal position from tidal signature matching

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.05970, 80.23996, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `TidalTimingPositionLayer` |
| Source | [`upin/layers/chemical/nautical.py`](../../upin/layers/chemical/nautical.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.40 |
| Novel | yes |

## How it works

```
Layer 83 — Coastal Position from Tidal Signature Matching.

Different coastal locations have unique tidal signatures
(amplitude, phase offset, harmonic mix). Record observed tide
pattern, match against database → coarse position estimate.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
