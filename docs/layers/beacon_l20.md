# Human Ground Beacon Network

`beacon_l20` · Layer 20 · Group J — Human & Crowd

> Encrypted human device ground reference beacons

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08262, 80.27025, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `HumanBeaconNetworkLayer` |
| Source | [`upin/layers/human/layers.py`](../../upin/layers/human/layers.py) |
| Capabilities | POSITION |
| Accuracy rating | 0.55 |
| Novel | yes |

## How it works

```
Layer 20 — Human Ground Beacon Network [NOVEL].

Personal devices (phones, wearables, tactical transponders)
triangulate own position via cell towers. Encrypted position
transmitted to UPIN as ground reference beacon. Invisible in
civilian mobile traffic.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
