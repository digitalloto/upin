# Ocean Current Drift Correction

`oceancurrent_h09` · Layer 82 · Group H — Chemical, Seismic & Flow

> Ocean current drift correction from pre-loaded current atlas

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.08409, 80.27035, 0.50 m/s, then a different position on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `OceanCurrentDriftLayer` |
| Source | [`upin/layers/chemical/nautical.py`](../../upin/layers/chemical/nautical.py) |
| Capabilities | POSITION, VELOCITY |
| Accuracy rating | 0.45 |
| Novel | yes |

## How it works

```
Layer 82 — Ocean Current Drift Correction.

Look up current vector at estimated position from pre-loaded atlas,
compute accumulated drift, subtract from dead reckoning position.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
