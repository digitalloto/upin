# Polynesian Wayfinding (Wave/Cloud/Bird)

`polynesian_e22` · Layer 100 · Group E — Optical & Vision

> Wave refraction + cloud patterns + bird behaviour for ocean nav

## Current status

**FABRICATES** — Invents a reading when nothing is connected, and invents a different one each time it is read.

Measured by constructing this layer with nothing attached and reading it three times: returned 13.07654, 80.28008, heading 172.9 deg, then a different position and heading on the very next read, from the same (absent) input.

With simulation switched off it raises NotImplementedError.

## What this layer needs

**Not yet declared.** This layer has no `REQUIRES` declaration, so the repo cannot say what hardware or data would bring it to life. Adding one is part of cleaning the layer up — see [`FABRICATION_AUDIT.md`](../../FABRICATION_AUDIT.md).

## Details

| | |
|---|---|
| Class | `PolynesianWayfindingLayer` |
| Source | [`upin/layers/maritime/classical_nav.py`](../../upin/layers/maritime/classical_nav.py) |
| Capabilities | POSITION, HEADING |
| Accuracy rating | 0.35 |
| Novel | yes |
| Bio-inspiration | Polynesian navigators — 3000 years of ocean wayfinding |

## How it works

```
Polynesian wayfinding — wave patterns, cloud reading, bird behaviour.

The cognitive fusion model: read swell refraction around islands,
lenticular clouds over peaks, green reflection over lagoons,
seabird flight at dawn/dusk. AI can learn these patterns.
```

---

*Generated by `tools/generate_layer_docs.py`. Do not edit by hand — regenerate instead. The status above is measured at generation time, not asserted.*
